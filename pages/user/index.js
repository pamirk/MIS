import React from "react";

function user({user}) {
    console.log("user", user)
    return (
        <div style={{minHeight: '100vh'}}>
            user
        </div>
    );
}

export default user;
